<UserControl x:Class="TreatmentPlanReport.Views.OrthogonalImageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TreatmentPlanReport.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="1.5*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Isocenter Orthogonal View" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" Grid.ColumnSpan="2"/>
            <Image Grid.Row="1" Grid.ColumnSpan="2" Source="{Binding TransverseImage}">

            </Image>
            <Image Grid.Row="2" Grid.Column="0" Source="{Binding FrontalImage}"/>
            <Image Grid.Row="2" Grid.Column="1" Source="{Binding SagittalImage}"/>
        </Grid>
    </Grid>
</UserControl>
