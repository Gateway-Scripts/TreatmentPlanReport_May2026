<UserControl x:Class="TreatmentPlanReport.Views.RxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TreatmentPlanReport.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Prescription Details" FontWeight="Bold"  Margin="10 2 2 2" Grid.ColumnSpan="2" Grid.Row="1"/>
        <Border BorderBrush="Black" BorderThickness="0 0 0 1" Grid.Row="1">
        </Border>
        <Border BorderBrush="Black" BorderThickness="0 0 0 1" Grid.Column="1" Grid.Row="1">
            <TextBlock Text="ARIA" FontWeight="Bold" Margin="10 2 2 2"/>
        </Border>
        <Border BorderBrush="Black" BorderThickness="0 0 0 1" Grid.Column="2" Grid.Row="1">
            <TextBlock Text="Eclipse" FontWeight="Bold" Margin="10 2 2 2"/>
        </Border>
        <ItemsControl ItemsSource="{Binding ReportItems}" Grid.ColumnSpan="3" Grid.Row="2">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Black" BorderThickness="0 1 0 0" >

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="2*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock FontWeight="Bold" Text="{Binding PropertyName}" Margin="10 2 2 2"/>
                            <TextBlock Text="{Binding AriaValue}" Margin="10 2 2 2" Grid.Column="1"/>
                            <TextBlock Text="{Binding EclipseValue}" Grid.Column="2" Margin="10 2 2 2"/>
                        </Grid>
                    </Border>

                </DataTemplate>

            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
