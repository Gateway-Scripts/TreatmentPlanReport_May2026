<Window x:Class="TreatmentPlanReport.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TreatmentPlanReport.Views"
        mc:Ignorable="d"
        Title="Treatment Plan Report" Height="850" Width="1200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel>
            <Button Content="Generate Report" Margin="10" Command="{Binding PrintReportCommand}"/>
            <Button Content="Post to ARIA" Margin="10" Command="{Binding PostARIACommand}"/>
        </StackPanel>
        <TabControl Margin="5" Grid.Row="1">
            <TabItem Header="Report">
                <StackPanel Margin="5">
                    <TextBlock Text="Patient Info" FontSize="14" FontWeight="Bold"/>
                    <local:PatientView DataContext="{Binding LocalPatientViewModel}"/>
                    <TextBlock Text="Plan Info" FontSize="14" FontWeight="Bold" Margin="0,10,0,0"/>
                    <local:PlanView DataContext="{Binding LocalPlanViewModel}"/>
                    <TextBlock Text="Prescription Info" FontSize="14" FontWeight="Bold" Margin="0,10,0,0"/>
                    <local:RxView DataContext="{Binding LocalRxViewModel}"/>
                    <TextBlock Text="Patient Shifts" FontSize="14" FontWeight="Bold" Margin="0,10,0,0"/>
                    <local:PatientShiftView DataContext="{Binding LocalPatientShiftViewModel}"/>
                </StackPanel>
            </TabItem>
            <TabItem Header="Fields">
                <StackPanel Margin="5">
                    <TextBlock Text="Field Info" FontSize="14" FontWeight="Bold"/>
                    <local:FieldSummaryView DataContext="{Binding LocalFieldViewModel}"/>
                </StackPanel>
            </TabItem>
            <TabItem Header="DVH">
                <local:DVHView DataContext="{Binding LocalDVHViewModel}"/>
            </TabItem>
            <TabItem Header="Iso Views">
                <local:OrthogonalImageView DataContext="{Binding LocalOrthogonalImageViewModel}"/>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
