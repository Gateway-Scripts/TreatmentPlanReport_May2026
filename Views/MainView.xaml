<Window x:Class="TreatmentPlanReport.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TreatmentPlanReport.Views"
        mc:Ignorable="d"
        Title="MainView" Height="450" Width="800">
    <Grid>
        <StackPanel Margin="5">
            <TextBlock Text="Patient Info" FontSize="14" FontWeight="Bold"/>
            <local:PatientView DataContext="{Binding LocalPatientViewModel}"/>
            <TextBlock Text="Plan Info" FontSize="14" FontWeight="Bold" Margin="0,10,0,0"/>
            <local:PlanView DataContext="{Binding LocalPlanViewModel}"/>
        </StackPanel>
    </Grid>
</Window>
